name: Run Tests
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          cd index-function
          pip install -r requirements-dev.txt
      - name: Run tests
        run: |
          pytest index-function/tests/ --cov=index-function --cov-report=xml